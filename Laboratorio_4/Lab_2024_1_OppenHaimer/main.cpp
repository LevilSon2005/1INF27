#include <iostream>
using namespace std;

int HallarRadiacionMaxima(int* arr,int inicio,int final) {
    if (inicio>final) return
}

int main() {
    int arr[]={0,-1,0,-1,0,1,1,2,2,3,2,2,1,1};
    int n= sizeof(arr)/sizeof(arr[0]);

    int radioacionMaxima = HallarRadiacionMaxima(arr,0,n-1);
    return 0;
}

